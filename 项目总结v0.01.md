# Hermes Agent WebUI - 项目总结文档

## 项目概述

Hermes Agent WebUI 是一个基于 Web 的智能 AI 助手界面，通过本地 Hermes Agent CLI 实现真正的终端对话交互，而非简单的 API 调用。项目采用前后端分离架构，后端通过 WSL2 调用 Hermes Agent CLI，前端通过 SSE（Server-Sent Events）实现流式实时渲染。

---

## 技术架构

### 整体架构图

```
┌─────────────────────────────────────────────────────┐
│                    前端 (Frontend)                    │
│  HTML + CSS + Vanilla JS (ES Modules)               │
│  - SSE 流式接收                                      │
│  - 实时 Markdown 渲染                                │
│  - 思考过程 / 工具调用 / Token 逐字显示               │
└──────────────────────┬──────────────────────────────┘
                       │ HTTP / SSE
                       ▼
┌─────────────────────────────────────────────────────┐
│                   后端 (Backend)                      │
│  Node.js + Express.js (端口 8787)                    │
│  - RESTful API 路由                                  │
│  - SSE 流式推送                                      │
│  - 子进程管理                                        │
└──────────────────────┬──────────────────────────────┘
                       │ spawn('wsl', [...])
                       ▼
┌─────────────────────────────────────────────────────┐
│              Hermes Agent CLI (v0.12.0)              │
│  WSL2 Ubuntu 环境                                    │
│  - hermes chat -q "..." -Q                          │
│  - 实时输出 token / reasoning / tool 事件            │
│  - 支持模型切换、技能注入、会话恢复                    │
└─────────────────────────────────────────────────────┘
```

### 请求流程

```
1. 用户输入消息
2. 前端 POST /api/chats/:id/messages (content)
3. 后端保存用户消息 → 构建 context → 调用 hermesStream()
4. hermesStream() 检测 Hermes CLI → spawn 子进程
5. 子进程通过 WSL2 执行: hermes chat -q "用户消息" -Q
6. 逐行解析 CLI 输出 → 生成事件 (token/reasoning/tool/error)
7. 后端通过 SSE 推送事件到前端
8. 前端实时渲染: 文本流 / 思考卡片 / 工具卡片
9. 流结束后保存助手消息到本地存储
```

---

## 目录结构

```
Hermes Agent/
├── backend/
│   ├── server.js              # Express 入口，路由挂载
│   ├── routes/
│   │   ├── chat.js            # 对话 CRUD + SSE 流式消息
│   │   ├── agent.js           # Agent 状态检测 API
│   │   ├── skills.js          # 技能管理
│   │   ├── models.js          # 模型配置
│   │   ├── settings.js        # 设置管理
│   │   ├── gateway.js         # 网关配置
│   │   ├── cron.js            # 定时任务
│   │   ├── usage.js           # 用量统计
│   │   └── system.js          # 系统信息
│   └── services/
│       ├── hermes.js          # ★ 核心：Hermes CLI 集成（WSL2 检测 + 流式解析）
│       ├── llm.js             # LLM 调用层（委托给 hermesStream）
│       └── store.js           # JSON 文件存储
├── frontend/
│   ├── index.html             # 主页面
│   ├── css/
│   │   ├── theme.css          # 语义化颜色变量 / 主题系统
│   │   ├── base.css           # 基础样式 / 工具类
│   │   ├── layout.css         # 布局系统
│   │   └── components.css     # 组件样式
│   └── js/
│       ├── main.js            # 启动入口
│       ├── api.js             # API 请求封装 + SSE 解析
│       ├── chat.js            # ★ 对话核心：消息渲染 / 流式回调 / 工具卡片
│       ├── settings.js        # 设置页面 + 连接检测
│       ├── agent.js           # Agent 状态页面
│       ├── skills.js          # 技能管理页面
│       ├── models.js          # 模型配置页面
│       ├── memory.js          # 记忆管理
│       ├── tasks.js           # 任务管理
│       ├── gateway.js         # 网关配置
│       ├── cron.js            # 定时任务
│       ├── usage.js           # 用量统计
│       ├── router.js          # 前端路由
│       └── store.js           # 前端状态管理
└── data/                      # 运行时数据目录
    ├── chats.json             # 对话数据
    ├── settings.json          # 设置数据
    ├── skills.json            # 技能数据
    └── history-md/            # 导出的 Markdown 历史
```

---

## 核心模块详解

### 1. Hermes CLI 集成 (`backend/services/hermes.js`)

**职责**：检测并调用 Hermes Agent CLI，解析流式输出

**检测策略（优先级从高到低）**：

| 优先级 | 策略 | 说明 |
|--------|------|------|
| 1 | 用户配置路径 | `settings.hermesPath` 自定义路径 |
| 2 | WSL2 | `wsl -e bash -lc 'hermes --version'` 检测 |
| 3 | 本地 hermes | Windows PATH 中的 hermes 命令 |

**WSL2 调用方式**：

```javascript
spawn('wsl', ['-e', 'bash', '-lc', "hermes chat -q '用户消息' -Q"])
```

**CLI 输出解析（正则匹配）**：

| 标签 | 正则 | 事件类型 | 说明 |
|------|------|----------|------|
| `[TOOL:START]` | `/^\[TOOL:START\]\s*(.*)/` | `tool` | 工具调用开始 |
| `[TOOL:END]` | `/^\[TOOL:END\]\s*(.*)/` | `tool_complete` | 工具调用完成 |
| `[REASONING]` | `/^\[REASONING\]\s*(.*)/` | `reasoning` | 推理/思考过程 |
| `[THINKING]` | `/^\[THINKING\]\s*(.*)/` | `reasoning` | 思考过程 |
| `[ERROR]` | `/^\[ERROR\]\s*(.*)/` | `error` | 错误信息 |
| `[TITLE]` | `/^\[TITLE\]\s*(.*)/` | `title` | 对话标题 |
| `[AGENT:xxx]` | `/^\[AGENT:\w+\]\s*/` | 忽略 | Agent 标签 |
| 其他文本 | — | `token` | 普通文本输出 |

### 2. SSE 流式通信 (`backend/routes/chat.js`)

**SSE 事件格式**：

```
event: token
data: {"text":"你好"}

event: reasoning
data: {"text":"让我思考一下..."}

event: tool
data: {"event_type":"tool.started","name":"read_file","preview":"读取文件","args":{}}

event: tool_complete
data: {"event_type":"tool.completed","name":"read_file","is_error":false,"duration":120}

event: title
data: {"title":"新对话标题","session_id":"xxx"}

event: error
data: {"msg":"错误信息"}

event: done
data: {"session_id":"xxx","usage":{"input_tokens":0,"output_tokens":0}}
```

### 3. 前端流式渲染 (`frontend/js/chat.js`)

**回调函数映射**：

| SSE 事件 | 回调 | 渲染行为 |
|----------|------|----------|
| `token` | `onToken(text)` | 追加文本 → Markdown 渲染 |
| `reasoning` | `onReasoning(text)` | 显示思考卡片（可折叠） |
| `tool` | `onTool(data)` | 显示工具调用卡片（⏳ 运行中） |
| `tool_complete` | `onToolComplete(data)` | 更新工具卡片状态（✅/❌） |
| `title` | `onTitle(data)` | 更新对话标题 |
| `done` | `onDone()` | 完成渲染，刷新列表 |
| `error` | `onError(msg)` | 显示错误信息 |

### 4. 主题系统 (`frontend/css/theme.css`)

**语义化颜色变量**：

```css
:root {
  /* 品牌色 */
  --brand: #6366f1;
  --brand-light: #818cf8;
  --brand-bg: rgba(99, 102, 241, 0.08);

  /* 文本色阶 */
  --text-1: #1a1a2e;    /* 主要文本 */
  --text-2: #64748b;    /* 次要文本 */
  --text-3: #94a3b8;    /* 辅助文本 */

  /* 背景色阶 */
  --bg-1: #ffffff;      /* 页面背景 */
  --bg-2: #f8fafc;      /* 区域背景 */
  --bg-3: #f1f5f9;      /* 卡片背景 */

  /* 语义色 */
  --success: #10b981;
  --warn: #f59e0b;
  --danger: #ef4444;
}

[data-theme="dark"] {
  --text-1: #e2e8f0;
  --text-2: #94a3b8;
  --text-3: #64748b;
  --bg-1: #0f172a;
  --bg-2: #1e293b;
  --bg-3: #334155;
}
```

---

## API 接口一览

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/health` | 健康检查 |
| GET | `/api/agent` | Agent 状态（含 Hermes CLI 检测） |
| GET | `/api/chats` | 对话列表 |
| POST | `/api/chats` | 创建对话 |
| GET | `/api/chats/:id` | 获取对话详情 |
| DELETE | `/api/chats/:id` | 删除对话 |
| PUT | `/api/chats/:id` | 更新对话（重命名等） |
| POST | `/api/chats/:id/messages` | 发送消息（SSE 流式响应） |
| GET | `/api/chats/:id/markdown` | 导出对话为 Markdown |
| GET | `/api/skills` | 技能列表 |
| POST | `/api/skills` | 添加技能 |
| PUT | `/api/skills/:id` | 更新技能 |
| DELETE | `/api/skills/:id` | 删除技能 |
| GET | `/api/models` | 模型配置 |
| PUT | `/api/models` | 更新模型配置 |
| GET | `/api/settings` | 获取设置 |
| PUT | `/api/settings` | 保存设置 |
| GET | `/api/gateway` | 网关配置 |
| PUT | `/api/gateway` | 更新网关 |
| GET | `/api/cron` | 定时任务列表 |
| POST | `/api/cron` | 创建定时任务 |
| PUT | `/api/cron/:id` | 更新定时任务 |
| DELETE | `/api/cron/:id` | 删除定时任务 |
| GET | `/api/usage` | 用量统计 |
| GET | `/api/system` | 系统信息 |

---

## 关键设计决策

### 为什么通过 WSL2 调用而不是直接 Python 集成？

| 方案 | 优点 | 缺点 |
|------|------|------|
| **WSL2 CLI 调用** ✅ | 无需安装 Python 依赖；与官方 CLI 行为一致；跨平台兼容 | 需要 WSL2 环境；进程启动有微小延迟 |
| Python 直接导入 | 性能更好；可复用 Agent 实例 | 需要安装 hermes-agent Python 包；依赖管理复杂 |
| OpenAI API 调用 | 简单直接 | 不是真正的 Hermes Agent；缺少工具调用等核心功能 |

### 为什么使用 SSE 而不是 WebSocket？

- SSE 是单向推送，与 LLM 流式输出场景天然匹配
- 原生 HTTP 协议，无需额外握手
- 自动重连机制
- 与参考项目 (hermes-webui) 保持一致

---

## 环境要求

| 组件 | 要求 | 说明 |
|------|------|------|
| Node.js | >= 16 | 后端运行时 |
| WSL2 | Ubuntu | Hermes Agent CLI 运行环境 |
| Hermes Agent | v0.12.0+ | WSL2 中安装 |
| 浏览器 | 现代浏览器 | 支持 SSE / ES Modules |

### Hermes Agent 安装（WSL2）

```bash
# 在 WSL2 Ubuntu 中执行
curl -fsSL https://raw.githubusercontent.com/NousResearch/hermes-agent/main/scripts/install.sh | bash

# 验证安装
hermes --version
# 输出: Hermes Agent v0.12.0 (2026.4.30)
```

### 启动项目

```bash
# 启动后端
cd backend
node server.js
# 输出: [hermes] backend listening on http://127.0.0.1:8787

# 前端由后端静态服务提供，直接访问 http://127.0.0.1:8787
```

---

## 参考项目

- **hermes-webui**: [https://github.com/nesquena/hermes-webui](https://github.com/nesquena/hermes-webui)
  - 参考了其 SSE 流式通信架构
  - 参考了消息渲染结构（思考过程、工具调用）
  - 参考了 Agent 状态检测机制
  - 参考了会话管理设计

---

## 已知限制与后续优化方向

1. **会话恢复**：当前不支持 Hermes CLI 的 `--resume` 参数恢复历史会话
2. **并发限制**：同一时间只能有一个 CLI 进程在运行
3. **取消机制**：前端 abort 只能断开 SSE 连接，无法终止 WSL 中的 CLI 进程
4. **编码问题**：Windows 终端显示 UTF-8 中文可能乱码，但数据传输正常
5. **Agent 缓存**：参考项目支持 Agent 实例缓存复用，当前每次请求创建新进程
