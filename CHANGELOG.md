# Changelog

## v1.4.0
- 优化设置页信息架构：保留顶部 Tab，并在“设置”页内增加卡片标题快速定位。
- 重构模型配置页：应用场景改为卡片选择，下方采用“获取模型 / 模型库”左右分栏。
- 优化聊天输入框：工具按钮改为纯图标，调整输入框高度、间距、模型选择按钮和发送区对齐。
- 修复设置页子页点击、模型配置打开和样式误显示为页面文本的问题。

## v1.3.0
- 新增 Hermes API Server 优先通道，失败时自动回退 Hermes CLI。
- 新增模型测速、失败回退模型、最快普通模型一键选择。
- 新增 Agent 反问弹窗协议，支持 Agent 暂停任务向用户确认后继续。
- 新增工具权限与安全审批，高风险命令需要弹窗确认。
- 新增更新中心，只检测 GitHub 远端状态，不自动覆盖本地文件。
- 新增一键备份导出，备份设置、模型、Skill、聊天索引和数据目录清单，敏感字段自动脱敏。
- 优化 Skill 触发词、优先级、自动触发词建议和命中原因显示。
- 优化弹窗视觉样式，统一标题、选项 hover/选中状态和底部按钮。
- 补充安装、更新、记忆路径、WebUI 逻辑和优化说明文档。

## v1.2.0
- 设置页新增 Agent 路由策略：自动、始终直连、始终 Hermes Agent。
- 设置页预留 Hermes API Server 地址和 API Key 配置入口。
- 后端记录每次聊天任务日志：路由、原因、耗时、输出长度和错误信息。
- 日志页升级为任务日志视图，优先展示最新任务和执行摘要。
- 保持普通聊天直连模型，复杂任务自动回退 Hermes Agent 的默认策略。

## v1.1.0
- 默认端口统一为 `3381`。
- 新增自动路由：普通聊天直连模型 API，复杂文件/命令/代码任务自动切换 Hermes Agent。
- 优化回复速度：普通聊天不再每次启动 Hermes CLI / WSL。
- 保留记忆和偏好注入：直连模型仍读取核心记忆、Agent 规则、会话历史和相关 Skill。
- Skill 改为按需注入，减少普通聊天 Prompt 长度。
- 修复消息流中断问题，避免 POST SSE 被误判为客户端中止。
- 优化执行过程展示，不再显示假的“思考中”，改为真实展示路由、首包、工具和完成事件。
- 修复 `<think>...</think>` 解析。
- 支持外部记忆、图片、Markdown 输出和历史目录配置。
- 新增 `update.bat`，方便通过 GitHub 拉取更新。
- 清理并重写 docs 文档，只保留当前有效说明。

## v1.0.0
- 统一并规范 Hermes WebUI 的整体视觉语言，偏向黑白极简风格。
- 调整右侧 Markdown 预览交互，优化拖拽手感并移除冗余提示文案。
- 重做消息操作区，统一为图标按钮并优化喜欢/不喜欢反馈样式。
- 改进思考块与工具调用块的展示方式，更接近 Claude / Codex 的轻量结构。
- 补充跨电脑安装支持：`start.ps1`、`start.sh`、`docker-compose.yml`、`Dockerfile`、`.env.example`。
- 让后端读取根目录 `.env`，便于本地和其他机器部署。
