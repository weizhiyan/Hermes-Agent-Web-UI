# Hermes Agent WebUI environment template
# Copy this file to .env when you need custom settings.

# Web server port
PORT=8787

# Optional: set to production for Docker/server deployments
NODE_ENV=production
