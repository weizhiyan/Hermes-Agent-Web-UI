# WebUI v1.5.3 修复对接文档

## 概述

本次修复了 10 个 WebUI 已知问题，涉及 4 个文件的逻辑修改 + 2 个新增本地依赖文件。

## 修改文件清单

| 文件 | 修改类型 |
|------|----------|
| `app-new.js` | 逻辑修改 + 新增函数 |
| `frontend/js/hermes-artifact.js` | 逻辑修改 + 渲染增强 |
| `backend/routes/chat.js` | 新增心跳机制 |
| `index.html` | CDN 改本地引用 |
| `frontend/js/marked.min.js` | 新增（从 node_modules 复制） |
| `frontend/js/highlight.min.js` | 新增（从 CDN 下载） |

## 新增依赖

需要在项目目录执行：
```bash
npm install marked highlight.js
```

---

## 逐项修改详情

### #10 — 对话列表排序改为降序

**文件：** `app-new.js`

**改动：**
- 函数 `compareChatCreatedAsc` 重命名为 `compareChatCreatedDesc`
- 排序方向反转：`chatCreatedTime(a)-chatCreatedTime(b)` → `chatCreatedTime(b)-chatCreatedTime(a)`
- ID 比较也反转：`String(a?.id).localeCompare(b)` → `String(b?.id).localeCompare(a)`
- 所有 6 处引用已全局替换
- 自动选择逻辑从 `state.chats[state.chats.length-1]` 改为 `state.chats[0]`（2 处）

**验证：** 刷新页面，最新对话应出现在列表顶部。

---

### #4 — Agent 对话刷新后丢失

**文件：** `app-new.js`

**改动：** `refreshChatSources()` 的 `webChats` 映射中，在 `agentId` 之后补充 4 个字段：

```javascript
agentName: c.agentName || '',
chatType: c.chatType || 'main',
isMainAgentChat: !!c.isMainAgentChat,
agentSnapshot: c.agentSnapshot || null,
```

**验证：** 打开 Agent 对话 → 刷新页面 → 点击同一 Agent → 应回到已有对话。

---

### #2 — 右侧预览面板点击卡片后渲染异常

**文件：** `frontend/js/hermes-artifact.js`

**改动：** `openRef()` 函数中，在 `layout = 'SPLIT_VIEW';` 之后添加一行：

```javascript
setTab('preview');
```

**验证：** 在知识库 tab 点击聊天中的 artifact 卡片 → 面板应切到预览 tab。

---

### #3 — 知识库箭头点击逻辑错误

**文件：** `frontend/js/hermes-artifact.js`

**改动 1：** `toggleHistoryCategoryMenu()` 重写 — 非 history tab 时只弹出分类浮层，不切换 tab：

```javascript
function toggleHistoryCategoryMenu() {
  historyCategoryMenuOpen = !historyCategoryMenuOpen;
  historyCategoryMenuPos = null;
  if (currentTab !== 'history') {
    if (!historyData) {
      loadHistory().then(() => renderToolbarCategoryMenu(
        historyData?.categories, historyData?.tags, historyData?.all));
    } else {
      renderToolbarCategoryMenu(
        historyData?.categories, historyData?.tags, historyData?.all);
    }
    return;
  }
  renderHistoryList();
}
```

**改动 2：** `setHistoryMode()` 添加 tab 切换 — 选择分类后才跳转：

```javascript
function setHistoryMode(mode) {
  historyMode = mode;
  historyPreview = null;
  historyCategoryMenuOpen = false;
  historyCategoryMenuPos = null;
  if (currentTab !== 'history') showHistory();  // 新增这行
  renderHistoryList();
}
```

**验证：** 预览 tab 点击知识库箭头 → 弹出分类菜单但不跳转 → 选择后才切换。

---

### #1 — Artifact 卡片自动检测（已回退）

**文件：** `app-new.js`

**改动：** `renderMarkdownFileCards()` 保持为空函数 `return ''`。不自动检测，依赖 Agent 使用 `<artifact>` 标签。

---

### #5 — Artifact 输出后聊天气泡空白

**文件：** `app-new.js`

**改动：** `renderMsg()` 中，原逻辑 `vis=mdCount?'':...` 替换为显示文档摘要：

```javascript
if(!vis&&(p.activeArtifact||(p.completedArtifacts||[]).length)){
  if(mdCount){
    const excerpts=(p.completedArtifacts||[]).filter(a=>
      String(a?.attrs?.type||'markdown').toLowerCase()==='markdown'
    ).map(a=>{
      const t=a.attrs?.title||'';
      const preview=String(a.content||'').replace(/<[^>]+>/g,'')
        .replace(/```[\s\S]*?```/g,'').replace(/[#*`>\-]/g,'')
        .trim().slice(0,100);
      return t?(t+'：'+preview):preview;
    }).filter(Boolean);
    vis=excerpts.length?excerpts.join('；')+'…':'已为你生成文档，可在右侧面板查看。';
  }else{
    vis='已为你生成文件，可在右侧面板或下方引用查看。';
  }
}
```

**验证：** Agent 输出 artifact → 聊天气泡应显示标题+前 100 字摘要，不再空白。

---

### #7 — 工具调用标签暴露给用户

**文件：** `app-new.js`

**改动 1：** 在 `renderMsg()` 之前新增 `stripRawToolCallTags` 函数：

```javascript
function stripRawToolCallTags(content, msg){
  if(!content||!/<tool_call[\s>]/.test(content)) return content;
  const tcRegex=/<tool_call>([\s\S]*?)<\/tool_call>/gi;
  let match;
  while((match=tcRegex.exec(content))!==null){
    const xml=match[1];
    const nameMatch=xml.match(/<name>([\s\S]*?)<\/name>/i);
    const paramMatch=xml.match(/<parameters>([\s\S]*?)<\/parameters>/i);
    const name=nameMatch?nameMatch[1].trim():'tool';
    let input='';
    if(paramMatch){
      try{input=JSON.stringify(JSON.parse(paramMatch[1].trim()),null,2)}
      catch(_){input=paramMatch[1].trim()}
    }
    if(msg){
      if(!msg.toolCalls) msg.toolCalls=[];
      msg.toolCalls.push({name,input,status:'success',duration:0});
    }
  }
  return content
    .replace(/<tool_call>[\s\S]*?<\/tool_call>/gi,'')
    .replace(/<\/?invoke>|<\/?parameter[^>]*>/gi,'')
    .trim();
}
```

**改动 2：** `renderMsg()` 中内容清洗阶段添加调用：

```javascript
// 在 content = content.replace(/⚠\s*Normalized model.../) 之后
if(m.role==='assistant') content = stripRawToolCallTags(content, m);
```

**验证：** Agent 输出含 `<tool_call>` 标签 → 自动转为折叠工具卡片，不显示原始 XML。

---

### #8 — Agent 工具执行失败无提示

**文件：** `app-new.js`

**改动：** `onToolComplete` 回调中，工具失败时弹出 toast：

```javascript
// 在 renderMsgUpdate(msgId, assistantMsg) 之前添加：
if(data.is_error) toast('工具 '+data.name+' 执行失败','error');
```

**验证：** 工具执行失败时，页面右上角应弹出红色错误提示。

---

### #9 — Agent 长任务静默中断

**文件：** `app-new.js` + `backend/routes/chat.js`

**前端改动：** `apiStream()` 的 reader 循环中，用 `Promise.race` 添加 60 秒超时：

```javascript
const STREAM_TIMEOUT_MS = 60000;
while (true) {
  const readPromise = reader.read();
  const timeoutPromise = new Promise((_, reject) => {
    setTimeout(() => reject(new Error('stream_timeout')), STREAM_TIMEOUT_MS);
  });
  let value, done;
  try {
    ({ value, done } = await Promise.race([readPromise, timeoutPromise]));
  } catch (e) {
    if (e.message === 'stream_timeout') {
      callbacks.onError?.('连接超时，Agent 可能已停止响应。请尝试重新发送。');
      break;
    }
    throw e;
  }
  if (done) break;
  // ... 后续处理不变
}
```

**后端改动：** 两个 SSE 端点（主聊天 + gc-stream）都添加 15 秒心跳：

```javascript
// 在 res.flushHeaders() 之后、res.on('close') 之前添加：
const heartbeat = setInterval(() => {
  try { res.write(':heartbeat\n\n'); } catch (_) { clearInterval(heartbeat); }
}, 15000);
// 在 res.on('close') 回调中添加：
clearInterval(heartbeat);
```

**验证：** Agent 长时间执行 → 60 秒无响应应弹出超时提示。

---

### #6 — 增量编辑内容丢失

**状态：** 前端流式接收已保证完整性，此问题根因在 Agent 侧上下文管理，WebUI 侧无法完全修复。

---

## marked / highlight.js 本地化

**改动：**
1. `npm install marked highlight.js` 安装到项目
2. 复制 `node_modules/marked/lib/marked.umd.js` → `frontend/js/marked.min.js`
3. 从 CDN 下载 `highlight.min.js` → `frontend/js/highlight.min.js`
4. `index.html` 中两行 `<script>` 从 CDN 改为本地路径

**`hermes-artifact.js` 渲染增强：**
- 新增 `getMarked()` 兼容函数，支持 `marked.parse` 和 `marked.marked` 两种 API
- marked 未加载时自动重试 5 次（每次间隔 500ms），而非直接降级为源码显示

**验证：** 断网环境下打开含 artifact 的对话 → 右侧面板应正常渲染 Markdown，而非显示源码。

---

## 新电脑环境搭建步骤

```bash
# 1. 进入项目目录
cd "f:\AI\Hermes Agent\WEB-UI"

# 2. 安装依赖（含新增的 marked 和 highlight.js）
npm install

# 3. 确认本地 JS 文件存在
ls frontend/js/marked.min.js      # ~43KB
ls frontend/js/highlight.min.js   # ~122KB

# 4. 启动后端
npm start

# 5. 浏览器打开 http://localhost:3381
```

## 注意事项

- `mermaid.min.js` 仍使用 CDN（文件 1.4MB 太大），断网时 Mermaid 图表无法渲染
- Issue #1（自动检测 MD 卡片）未实现，依赖 Agent 用 `<artifact>` 标签输出文档
- Issue #6（增量编辑丢失）根因在 Agent 侧，WebUI 流式接收已保证完整
- 所有改动为增量修改，不影响原有功能逻辑
